# Copy this file to `.env.local` (gitignored) and fill in real values.
# `.env.local` is read by tools/gen-art.mjs for Nano Banana image generation.

# Google AI Studio API key (Nano Banana / Gemini 2.5 Flash Image).
# Get one at https://aistudio.google.com → API keys. Free tier usually
# covers the initial UI asset pack; upgrade only if you hit a limit.
GOOGLE_API_KEY=
